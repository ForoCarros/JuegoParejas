package control;

public interface ParaUIable {
	/**
	 * Actualiza las cartas en la ventana. Si oculta, no las muestra. Si !oculta, las
	 * muestra. Si emparejadas, no las oculta una vez mostradas.
	 */
	public void actualizarVentana();
}
